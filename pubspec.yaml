name: emerald_api
description: Emerald API client for Dart and Flutter — gRPC stubs and a thin channel wrapper.
version: 0.1.0-dev
repository: https://github.com/emeraldpay/emerald-api-dart

environment:
  sdk: ^3.11.5

dependencies:
  grpc: ^5.0.0
  protobuf: ^6.0.0
  fixnum: ^1.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  protoc_plugin: ^25.0.0
